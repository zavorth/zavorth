apiVersion: v1
kind: Secret
metadata:
  name: zavorth-secrets
  labels:
    app: zavorth
type: Opaque
data:
  # Base64-encoded placeholders — replace with real values
  # echo -n 'your-value' | base64
  API_KEY: ""
  DATABASE_URL: ""
  REDIS_URL: ""
  JWT_SECRET: ""

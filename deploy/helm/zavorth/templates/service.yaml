apiVersion: v1
kind: Service
metadata:
  name: {{ include "zavorth.fullname" . }}
  labels:
    {{- include "zavorth.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "zavorth.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http

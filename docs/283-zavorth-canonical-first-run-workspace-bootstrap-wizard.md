# Zavorth Canonical First-Run Workspace Bootstrap Wizard

Status: ready

Supported paths:
- `zavorth setup --dry-run`
- `zavorth setup`

The wizard records user display name, agent display name, tone, workspace root,
provider status, memory mode and safety posture without storing raw secrets.
Runtime profile evidence is local-only and ignored by Git.

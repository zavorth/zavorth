# 159 - External Executor Secret Provisioning Pause

Secret provisioning remains paused unless a governed Zavorth-native SecretRef
path is used. No raw secret migration is authorized by these packs.

- `238` executes the first controlled real message send under a narrow test gate.
- `190` is the native registry parity and dependency-reduction gate.
- `189` is the native config/secrets/state metadata registry slice.
- `196` is the native registry sandbox restore/load path.
- `198` is the flagged native registry production persistence gate.
- `205` is the native registry consumer expansion pack.
- `209` opens Wave 4A controlled metadata/config/registry migration planning.
- `217` is the Wave 4B low-risk executable capabilities milestone report.
- `231` executes the first Wave 4B.3 dry-run executable.
- `240` consolidates Wave 4D message send without a new send.
- `241` opens Wave 4E provider execution absorption.
- `242` opens Wave 4F tool/command execution absorption.
- `243` opens Wave 5 final adapter domain decommission.
- `244` closes the absorption track with Zavorth-only hardening.

---
name: Airtable Ops
description: Airtable base operations and automations
license: Zavorth-Internal
---

# Airtable Ops

Use this native skill when:
- The task requires operations in the 'productivity' domain.
- Performing actions matching: Airtable base operations, views, automations.

## Operating Rules

- Use Airtable REST API with proper base IDs, table IDs, and API key authentication.
- Perform CRUD operations on records with proper field type handling (attachments, lookups, formulas).
- Create filtered views with sort, field visibility, and grouping configurations.
- Handle pagination with offset tokens for tables exceeding 100 records per request.
- Map Airtable field types to appropriate data structures for programmatic access.

## Output

- Airtable API scripts, record data exports, and automation configurations.

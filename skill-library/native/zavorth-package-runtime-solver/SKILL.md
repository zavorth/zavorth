---
name: Dependency Solver
description: Solve package dependency conflicts, missing runtimes, and lockfile errors locally.
license: Zavorth-Internal
---

# Dependency Solver

Use this native skill when:
- The task requires operations in the 'devops' domain.
- Performing actions matching: solve package dependency conflicts, missing runtimes, and lockfile errors locally.

## Operating Rules

- Scan for missing libraries or package manager locks.
- Propose automated lockfile updates or dependency resolution plans.
- Verify compiler or build constraints before writing changes.

## Output

Return clean resolution plans, command execution lists, and a fixed lockfile verification.

# Plan

1. Enriquecer o snapshot de governanca por tenant com actions e recipes derivados do status.
2. Reaproveitar a surface textual compartilhada para expor os recipes em `/tenants`.
3. Expor `featuredRecipes` no `/app` com guided actions reais e fallback de comando.
4. Cobrir backend, endpoint protegido e asset web com testes focados.

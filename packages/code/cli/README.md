# @zavorth/cli

Zavorth Code — terminal-native AI coding assistant (TUI + server).

## Dev

```bash
# from monorepo root
bun install
bun run --cwd packages/cli dev
```

## Build

```bash
bun run --cwd packages/cli build
```

Binary lands under `packages/cli/dist/`.

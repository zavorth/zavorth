# linear

Linear GraphQL bridge for Plugin OS.

## Capabilities

- `linear.status`
- `linear.issues.list` — `{ limit? }`
- `linear.issue.create` — `{ title, description? }` requires `approved=true`

Env: `LINEAR_API_KEY`

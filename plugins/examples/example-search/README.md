# example-search

Minimal Plugin OS **search** example. Binds `search.query` and returns empty results (no network).

```bash
zavorth plugins install ./plugins/examples/example-search --yes
zavorth plugins enable example-search --yes
zavorth plugins test ./plugins/examples/example-search
```

# example-voice

Minimal Plugin OS **voice** example. Binds `voice.run` as a local stub (no STT/TTS network).

```bash
zavorth plugins install ./plugins/examples/example-voice --yes
zavorth plugins enable example-voice --yes
zavorth plugins test ./plugins/examples/example-voice
```

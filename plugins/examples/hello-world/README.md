# hello-world

Minimal Plugin OS **tool** example. Binds `main.run` and echoes input.

```bash
zavorth plugins install ./plugins/examples/hello-world --yes
zavorth plugins enable hello-world --yes
zavorth plugins test ./plugins/examples/hello-world
```

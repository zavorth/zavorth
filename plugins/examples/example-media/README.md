# example-media

Minimal Plugin OS **media** example. Binds `media.run` as a local stub (no network I/O).

```bash
zavorth plugins install ./plugins/examples/example-media --yes
zavorth plugins enable example-media --yes
zavorth plugins test ./plugins/examples/example-media
```

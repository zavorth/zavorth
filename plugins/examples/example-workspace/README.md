# example-workspace

Minimal Plugin OS **workspace** example. Binds `workspace.info` and reports the workspace path when available.

```bash
zavorth plugins install ./plugins/examples/example-workspace --yes
zavorth plugins enable example-workspace --yes
zavorth plugins test ./plugins/examples/example-workspace
```

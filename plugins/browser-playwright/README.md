# browser-playwright

Playwright browser tool for Zavorth Plugin OS.

## Capabilities

- `browser.status`
- `browser.open` — `{ url }`

Soft-fail when `BrowserPlaywrightService` or Playwright is missing.

## Enable

```bash
zavorth plugins enable browser-playwright --yes
```

{
  "name": "zavorth-agent",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Zavorth Agent — Lightweight voice process with Wake Word + Hotkey residing in the system tray",
  "main": "src/index.ts",
  "scripts": {
    "start": "npx tsx src/index.ts",
    "dev": "npx tsx watch src/index.ts",
    "typecheck": "tsc --noEmit",
    "smoke:echo": "npx tsx src/EchoClientService.smoke.ts",
    "smoke:nexus": "npm run smoke:echo",
    "smoke:voice": "npx tsx src/AgentVoiceFlow.smoke.ts"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "node-notifier": "^10.0.1",
    "systray2": "^2.1.4"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "~5.6.0"
  },
  "overrides": {
    "esbuild": "^0.28.1",
    "uuid": "^11.1.1"
  }
}

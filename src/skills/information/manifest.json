{
  "id": "information",
  "name": "Information & Web Search",
  "version": "1.0.0",
  "description": "Tools for searching the internet and obtaining the current date/time.",
  "category": "information",
  "dangerLevel": "low",
  "tools": [
    {
      "name": "web_search",
      "description": "Searches current internet information through DuckDuckGo and RSS fallbacks.",
      "parameters": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "A consulta de busca."
          },
          "limit": {
            "type": "number",
            "description": "Maximum number of results (default: 3, max: 5)."
          }
        },
        "required": ["query"]
      }
    },
    {
      "name": "get_datetime",
      "description": "Retorna a data e hora current do sistema local.",
      "parameters": {
        "type": "object",
        "properties": {
          "timezone": {
            "type": "string",
            "description": "Time zone, for example America/Sao_Paulo. Default: local."
          }
        }
      }
    }
  ],
  "metadata": {
    "author": "zavorth-core",
    "firewall_category": "information",
    "requires_sandbox": false
  }
}

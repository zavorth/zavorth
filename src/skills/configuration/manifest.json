{
  "id": "configuration",
  "name": "Configuration & LLM Profile",
  "version": "1.0.0",
  "description": "Tools to configure and switch LLM models/providers.",
  "category": "configuration",
  "dangerLevel": "low",
  "tools": [
    {
      "name": "configure_llm_profile",
      "description": "Lists, negotiates, and switches the active Zavorth LLM provider/model.",
      "parameters": {
        "type": "object",
        "properties": {
          "action": {
            "type": "string",
            "description": "Action: list, set, suggest, negotiate.",
            "enum": ["list", "set", "suggest", "negotiate"]
          },
          "provider": {
            "type": "string",
            "description": "Provider name (gemini, openai, deepseek, etc.)."
          },
          "model": {
            "type": "string",
            "description": "Specific model name."
          }
        },
        "required": ["action"]
      }
    }
  ],
  "metadata": {
    "author": "zavorth-core",
    "firewall_category": "configuration",
    "requires_sandbox": false
  }
}

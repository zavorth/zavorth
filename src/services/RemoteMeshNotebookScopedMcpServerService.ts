/**
 * @deprecated Import from `@zavorth/mesh/RemoteMeshNotebookScopedMcpServerService.js`.
 *
 * This compatibility module intentionally has no implementation. Keeping the
 * server in one canonical location prevents security fixes from drifting across
 * duplicate Remote Mesh implementations.
 */
export * from '../mesh/RemoteMeshNotebookScopedMcpServerService.js';
